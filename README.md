# Exam Project

## Backend

### Requirements

- Python 3.8+
- FastAPI
- Uvicorn

### Setup

1. Navigate to the `backend` directory:
   ```bash
   cd backend
   pip install
   uvicorn app.main:app --reload
   ```
